<Window x:Class="WfpTrafficControl.UI.Views.PolicyDiffView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WfpTrafficControl.UI.ViewModels"
        xmlns:converters="clr-namespace:WfpTrafficControl.UI.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:PolicyDiffViewModel}"
        Title="Policy Comparison"
        Width="900" Height="700"
        MinWidth="700" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- Change type to color converter -->
        <SolidColorBrush x:Key="AddedBrush" Color="#22C55E" />
        <SolidColorBrush x:Key="RemovedBrush" Color="#EF4444" />
        <SolidColorBrush x:Key="ModifiedBrush" Color="#F59E0B" />
        <SolidColorBrush x:Key="UnchangedBrush" Color="#6B7280" />

        <SolidColorBrush x:Key="AddedBackgroundBrush" Color="#F0FDF4" />
        <SolidColorBrush x:Key="RemovedBackgroundBrush" Color="#FEF2F2" />
        <SolidColorBrush x:Key="ModifiedBackgroundBrush" Color="#FFFBEB" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <!-- Header with policy selection -->
            <RowDefinition Height="Auto" />
            <!-- Summary -->
            <RowDefinition Height="Auto" />
            <!-- Diff content -->
            <RowDefinition Height="*" />
            <!-- Footer -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header: Policy Selection -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Policy -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Baseline (Left)"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />
                    <Border Background="{DynamicResource InputBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding LeftPolicyName}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding LeftPolicyPath}"
                                       VerticalAlignment="Center" />
                            <Button Grid.Column="1"
                                    Content="Browse..."
                                    Command="{Binding LoadLeftPolicyCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="12,4"
                                    Margin="8,0,0,0" />
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Swap Button -->
                <Button Grid.Column="1"
                        Content="Swap"
                        Command="{Binding SwapPoliciesCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Padding="16,8"
                        Margin="16,20,16,0"
                        ToolTip="Swap left and right policies" />

                <!-- Right Policy -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="New (Right)"
                               FontWeight="SemiBold"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />
                    <Border Background="{DynamicResource InputBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding RightPolicyName}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding RightPolicyPath}"
                                       VerticalAlignment="Center" />
                            <Button Grid.Column="1"
                                    Content="Browse..."
                                    Command="{Binding LoadRightPolicyCommand}"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="12,4"
                                    Margin="8,0,0,0" />
                        </Grid>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Bar -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding DiffSummary}"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />

                <!-- Legend -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Border Width="12" Height="12" Background="{StaticResource AddedBrush}" CornerRadius="2" />
                        <TextBlock Text="Added" Margin="6,0,0,0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Border Width="12" Height="12" Background="{StaticResource RemovedBrush}" CornerRadius="2" />
                        <TextBlock Text="Removed" Margin="6,0,0,0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Border Width="12" Height="12" Background="{StaticResource ModifiedBrush}" CornerRadius="2" />
                        <TextBlock Text="Modified" Margin="6,0,0,0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="12" Height="12" Background="{StaticResource UnchangedBrush}" CornerRadius="2" />
                        <TextBlock Text="Unchanged" Margin="6,0,0,0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Diff Content -->
        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8"
                Padding="0">
            <Grid>
                <!-- Diff List -->
                <ListView ItemsSource="{Binding DiffItems}"
                          BorderThickness="0"
                          Background="Transparent"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChangeType}" Value="Added">
                                                <Setter Property="Background" Value="{StaticResource AddedBackgroundBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ChangeType}" Value="Removed">
                                                <Setter Property="Background" Value="{StaticResource RemovedBackgroundBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ChangeType}" Value="Modified">
                                                <Setter Property="Background" Value="{StaticResource ModifiedBackgroundBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Change Indicator -->
                                <Border Grid.Column="0"
                                        Padding="8,12">
                                    <TextBlock Text="{Binding ChangeIndicator}"
                                               FontFamily="Consolas"
                                               FontWeight="Bold"
                                               FontSize="16"
                                               HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource UnchangedBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="Added">
                                                        <Setter Property="Foreground" Value="{StaticResource AddedBrush}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="Removed">
                                                        <Setter Property="Foreground" Value="{StaticResource RemovedBrush}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="Modified">
                                                        <Setter Property="Foreground" Value="{StaticResource ModifiedBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>

                                <!-- Rule Details -->
                                <StackPanel Grid.Column="1" Margin="0,8,16,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding RuleId}"
                                                   FontWeight="SemiBold"
                                                   FontFamily="Consolas" />
                                        <TextBlock Text=" — "
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                        <TextBlock Text="{Binding Description}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Details}"
                                               FontSize="11"
                                               FontFamily="Consolas"
                                               Margin="0,4,0,0"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="Unchanged">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Empty State -->
                <TextBlock Text="Load two policies to see comparison"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="14">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DiffItems.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Loading Overlay -->
                <Border Background="#80FFFFFF"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Loading policy..."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="16"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                </Border>
            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Clear All"
                    Command="{Binding ClearAllCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Padding="16,8"
                    HorizontalAlignment="Left" />

            <Button Grid.Column="1"
                    Content="Close"
                    IsCancel="True"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Padding="24,8" />
        </Grid>
    </Grid>
</Window>
